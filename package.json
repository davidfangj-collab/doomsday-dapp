{
  "name": "doomsday-dapp",
  "version": "1.0.0",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy:sepolia": "hardhat run scripts/deploy-messageboard.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy-messageboard.js --network localhost",
    "start": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "ethers": "^6.9.0"
  }
}
