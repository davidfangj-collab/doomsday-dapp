# 🚀 快速启动指南

## 📋 准备工作（5 分钟）

### 1. 获取 Sepolia RPC URL

访问 [Alchemy](https://www.alchemy.com/)：
1. 注册/登录账号
2. 点击 "Create App"
3. 选择 **Ethereum** → **Sepolia**
4. 复制 **HTTPS URL**（类似：`https://eth-sepolia.g.alchemy.com/v2/xxxxx`）

### 2. 获取测试币

访问 [Sepolia Faucet](https://sepoliafaucet.com/)：
1. 输入你的钱包地址
2. 完成验证
3. 等待接收测试币（需要至少 0.1 ETH）

### 3. 导出 MetaMask 私钥

⚠️ **重要：这是测试网私钥，不要用于主网！**

1. 打开 MetaMask
2. 点击右上角 **三个点** → **账户详情**
3. 点击 **导出私钥**
4. 输入密码
5. 复制私钥（以 `0x` 开头的 64 位字符串）

## 🎯 一键启动（3 步）

### 方法 1: 使用启动脚本（推荐）

```powershell
# 1. 运行启动脚本
.\start.ps1

# 2. 选择选项 4 (部署并启动)
# 脚本会自动：
#   - 编译合约
#   - 部署到 Sepolia
#   - 启动前端
```

### 方法 2: 手动执行

```bash
# 1. 安装依赖
npm install
cd frontend && npm install && cd ..

# 2. 配置环境变量
# 复制 .env.template 为 .env，然后编辑：
SEPOLIA_RPC_URL=你的_RPC_URL
PRIVATE_KEY=你的_私钥

# 3. 编译合约
npm run compile

# 4. 部署到 Sepolia
npm run deploy:sepolia

# 5. 启动前端
npm start
```

## 🎮 使用 DApp

### 1. 连接钱包

1. 打开浏览器访问 http://localhost:3000
2. 在 MetaMask 中切换到 **Sepolia 测试网**
3. 点击页面右上角 **"连接钱包"**
4. 在 MetaMask 中确认连接

### 2. 发布留言

1. 在文本框输入留言内容（最多 280 字符）
2. 设置竞价金额（必须大于当前最低竞价）
3. 点击 **"发布留言"**
4. 在 MetaMask 中确认交易
5. 等待交易确认（约 15-30 秒）

### 3. 查看排名

- 留言按竞价金额降序排列
- 前 3 名有特殊徽章显示
- 每 24 小时竞价自动衰减 50%
- 只显示前 100 条留言

## 🌐 部署到 Vercel

### 1. 推送到 GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/你的用户名/doomsday-dapp.git
git push -u origin main
```

### 2. 在 Vercel 部署

1. 访问 [Vercel](https://vercel.com/)
2. 点击 **"Import Project"**
3. 选择你的 GitHub 仓库
4. Vercel 会自动检测 `vercel.json` 配置
5. 点击 **"Deploy"**
6. 等待部署完成（约 2-3 分钟）

### 3. 访问你的 DApp

部署完成后，Vercel 会提供一个域名，例如：
- `https://doomsday-dapp.vercel.app`

## 🐛 常见问题

### 问题 1: 部署失败 - 余额不足

**解决方案：**
- 确保钱包有至少 0.1 ETH
- 从水龙头获取更多测试币

### 问题 2: 前端无法连接合约

**解决方案：**
1. 确认 MetaMask 已切换到 Sepolia 网络
2. 检查 `frontend/src/App.jsx` 中的 `CONTRACT_ADDRESS` 是否正确
3. 清除浏览器缓存并刷新

### 问题 3: 交易失败

**解决方案：**
- 检查竞价金额是否大于最低竞价
- 确保钱包有足够的 ETH 支付 Gas 费
- 尝试增加 Gas 限制

### 问题 4: Vercel 部署 404

**解决方案：**
- 确保 `vercel.json` 文件存在
- 检查构建日志是否有错误
- 确认输出目录为 `frontend/dist`

## 📊 项目状态检查

```bash
# 检查合约是否编译
ls artifacts/contracts/MessageBoard.sol/MessageBoard.json

# 检查前端依赖
cd frontend && npm list

# 测试合约
npm test

# 查看部署的合约
# 访问 https://sepolia.etherscan.io/address/你的合约地址
```

## 💡 提示

- 💰 测试网交易免费，但需要测试币
- ⏱️ 交易确认通常需要 15-30 秒
- 🔄 如果交易卡住，可以在 MetaMask 中加速
- 📱 确保 MetaMask 已解锁并连接到正确的网络
- 🔐 永远不要分享你的私钥

## 📚 更多资源

- [完整部署指南](./DEPLOYMENT_GUIDE.md)
- [项目 README](./README.md)
- [Hardhat 文档](https://hardhat.org/docs)
- [Ethers.js 文档](https://docs.ethers.org/)

---

🔥 **准备好了吗？运行 `.\start.ps1` 开始吧！**

